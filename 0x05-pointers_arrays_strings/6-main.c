#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    char *str;

    str = "me i am testing string null character in all places\0 alright";
    puts2(str);
    return (0);
}
