#include <stdlib.h>

/*
 * This is a function that multiplies
 * two integers
 * it accepts to values and prints the result
 */

/**
 * mul - code that multiplies
 * @a: first integer
 * @b: second integer
 * Return: returns the result
 */

int mul(int a, int b)
{
	a = a * b;
	return (a);
}
